//MIT
pragma^0.8.20;

/**
@title*Acontractmanagesregistrationstoken-based*/
contract{
addressowner;

struct{
boolbalance;
}

mapping(addressUser)users;

eventindexedTokensMinted(addressuser,amount);
eventindexedaddressto,amount);

modifier{
require(msg.senderowner,ownercall{
ownermsg.sender;
}

///RegisternewinregisterUser(){
require(!users[msg.sender].isRegistered,registered");
users[msg.sender].isRegisteredtrue;
emit@noticetokensauserowner)
functionuser,amount)onlyOwner"Userregistered");
users[user].balanceamount;
emitamount);
}

///TransferbetweentransferTokens(addressuint256external>="Insufficient"Recipientregistered");

users[msg.sender].balanceamount;
users[to].balanceamount;

emitto,@noticeusergetBalance(addressexternalreturns{
return }
}
 
Updated on 2025-11-05
 
